/*
import {SuperSpan} from "./SuperSpan";
import {ComponentMeta, ComponentStory} from "@storybook/react";
import {action} from "@storybook/addon-actions";

export default {
    title: 'TODOLIST/SuperSpan Component',
    component: SuperSpan,
    argTypes: { changeTitle: { action: 'changed' } }
} as ComponentMeta<typeof SuperSpan>;

const changeCallback = action('Button \'add\' was pressed inside the form')

const Template: ComponentStory<typeof SuperSpan> = (args) => <SuperSpan {...args} />;

export const SuperSpanBaseExample = Template.bind({});
SuperSpanBaseExample.args = {
    title: 'start value',
    changeTitle: changeCallback,
}*/
export {}