/*import {SuperInput} from "./SuperInput";
import {ComponentMeta, ComponentStory} from "@storybook/react";
import {action} from "@storybook/addon-actions";

export default {
    title: 'TODOLIST/SuperInput Component',
    component: SuperInput,
} as ComponentMeta<typeof SuperInput>;

const callback = action('Button \'add\' was pressed inside the form')

const Template: ComponentStory<typeof SuperInput> = (args) => <SuperInput {...args} />;

export const SuperInputBaseExample = Template.bind({});
SuperInputBaseExample.args = {
    addHandler: callback,
}*/
export {}
